CATATAN REFACTOR SIPILMU

File utama yang diperbaiki:
1. style.css
   - Refactor responsive mobile-first.
   - Pengaman anti horizontal scroll.
   - Grid card auto-fit minmax 240px.
   - Form dan tombol dibuat nyaman untuk layar HP.
   - Tabel hasil diberi horizontal scroll aman.
   - Bottom navigation dibuat aman untuk Android dan iPhone dengan safe area.
   - Fallback ditambahkan untuk color-mix dan backdrop-filter.
   - Dark mode tetap memakai variabel warna yang sama.

2. script.js
   - Menu mobile dibuat lebih stabil.
   - Tombol menu sekarang mengubah aria-label, aria-expanded, dan ikon dengan benar.
   - Menu bisa tertutup saat klik link, klik area luar, atau tekan Escape.
   - Hasil perhitungan otomatis diarahkan ke kartu hasil pada layar kecil.

3. file HTML
   - Gambar logo diberi width, height, decoding, dan fetchpriority.
   - Gambar QR pada halaman sawer diberi loading lazy, decoding async, width, dan height.

Checklist internal:
- JavaScript lolos pengecekan sintaks node --check.
- Semua HTML memiliki viewport, CSS, dan script utama.
- Gambar QR sudah loading lazy.
- Fallback CSS modern sudah tersedia.
